<section id="banner" class="banner">  
    <div class="container">
        
        <div id="ghughu-banner-carousel" class="carousel slide carousel-fade" data-ride="carousel">

        <!-- Indicators -->
            <ol class="carousel-indicators">
            {% for i in  0..3 %}
                <li data-target="#ghughu-banner-carousel" data-slide-to="{{i}}" class="{{loop.index is same as (0) ? 'active'}}"></li>
                {# <li data-target="#ghughu-banner-carousel" data-slide-to="{{1}}"></li>
                <li data-target="#ghughu-banner-carousel" data-slide-to="2"></li> #}
            {% endfor %}
                
            </ol>
            
            <!-- Wrapper for slides -->
            <div class="carousel-inner full-height" role="listbox">
            {% for item in slider %}
                <div class="item {{ loop.index is same as(1) ? 'active'}} banner-item">
                    <div class="row">
                        <div class="col-sm-7">
                            <h1 class="animated fadeInUp delay-2">{{item.title}}</h1>
                            <p class="animated fadeInUp delay-3">{{item.description | raw}}.</p>
                        </div>
                        <div class="col-sm-5 text-center hidden-xs">
                            <img src="images/banner-carousel/{{item.image}}" class="animated fadeInUp delay-4 img-responsive" alt="banner">
                        </div>
                    </div>
                </div>
            {% endfor %}    
            </div>  
        </div>
        <!-- Controls -->
        <a class="left carousel-control" href="#ghughu-banner-carousel" role="button" data-slide="prev">
        <i><img src="images/banner-carousel/arrow-left.svg" alt="icons"></i>
        </a>
        <a class="right carousel-control" href="#ghughu-banner-carousel" role="button" data-slide="next">
        <i><img src="images/banner-carousel/arrow-right.svg" alt="banner"></i>
        </a>    
    </div> 
</section>

<section id="who-are-we" class="who-are-we page">
    <div class="container wow fadeInUp">
        <div class="row">
            <div class="col-sm-5 col-md-6">
                <h1>Qui sommes-nous ?</h1>
            </div>
            {% for item in whoareyou %}
                <div class="col-sm-7 col-md-6">
                    <p>
                        {{item.description | raw}}.
                    </p>
                </div>
            {% endfor %}
            
        </div>
    </div>
</section>

<!-- most probably like canvas -->
<section id="services" class="services page">
    <div class="container wow fadeInUp">
        <div class="row">
            <div class="col-md-12 main-heading">
                <h1>Nos Services</h1>  
            </div>
        {% for item in services %}
            <div class="col-md-12">
                <div class="row">
                    <div class="service clearfix">
                        <div class="col-sm-4 col-md-5">
                            <span class="service-number">{{loop.index}} </span>
                        </div>
                        <div class="col-sm-8 col-md-7">
                            <h2>{{item.name}}</h2>
                            <p>{{item.description | raw}}</p>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
    </div>            
</section>
<!-- CTA section starts -->
        <section id="cta" class="cta page">
            <div class="container wow fadeInUp">
                <div class="row">
                    <div class="col-sm-6">
                        <h1>Laissez nous vous aider ..</h1>
                    </div>
                    <div class="col-sm-6">
                        <button href="{{path('app_contact')}} " class="btn btn-block custom-button">Contactez-nous</button>
                    </div>
                </div><!-- End: .row -->
            </div><!-- End: .container-->
        </section>
        <!-- CTA section ends -->